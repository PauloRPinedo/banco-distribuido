# A única dependência externa do projeto, decidida pelo grupo em setembro de 2026.
# A justificação está em docs/SPECS.md 11.4 e em docs/CONVENCOES.md secção 4.
#
#   pip install -r requisitos.txt
#
# [binary] traz a libpq já compilada: sem isto seria preciso um compilador e o
# libpq-dev em cada laptop, que é exatamente o género de obstáculo que este
# projeto evita.
#
# Os testes NÃO precisam disto: `python3 -m unittest discover -s tests` corre
# numa máquina limpa, com o armazém em memória.
psycopg[binary]>=3.1
